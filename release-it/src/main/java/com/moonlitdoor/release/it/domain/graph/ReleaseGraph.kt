package com.moonlitdoor.release.it.domain.graph

class ReleaseGraph(
  val id: String,
  val name: String?,
  val draft: Boolean,
  val description: String?
) {
  companion object {

//    fun from(node: GithubQuery.Node1) = ReleaseGraph(
//      id = node.id(),
//      name = node.name(),
//      draft = node.isDraft,
//      description = node.description()
//    )
//
//    fun from(node: GithubQuery.Node4) = ReleaseGraph(
//      id = node.id(),
//      name = node.name(),
//      draft = node.isDraft,
//      description = node.description()
//    )
//
//    fun from(node: ViewerRepositoriesAfterQuery.Node1) = ReleaseGraph(
//      id = node.id(),
//      name = node.name(),
//      draft = node.isDraft,
//      description = node.description()
//    )
//
//    fun from(node: OrganizationsAfterQuery.Node2) = ReleaseGraph(
//      id = node.id(),
//      name = node.name(),
//      draft = node.isDraft,
//      description = node.description()
//    )
//
//    fun from(node: OrganizationRepositoriesAfterQuery.Node1) = ReleaseGraph(
//      id = node.id(),
//      name = node.name(),
//      draft = node.isDraft,
//      description = node.description()
//    )
//
//    fun from(node: ReleasesAfterQuery.Node) = ReleaseGraph(
//      id = node.id(),
//      name = node.name(),
//      draft = node.isDraft,
//      description = node.description()
//    )
  }
}